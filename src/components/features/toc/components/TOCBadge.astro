---
/**
 * TOC 徽章组件
 * 支持数字、圆点、小圆点三种变体
 */

export type BadgeVariant = "number" | "dot" | "dot-small";

export interface Props {
	/** 徽章文本（仅 number 变体使用） */
	text?: string;
	/** 徽章变体类型 */
	variant?: BadgeVariant;
	/** 是否激活状态 */
	active?: boolean;
}

const { text = "", variant = "number", active = false } = Astro.props;
---

{
	variant === "number" && (
		<span class:list={["toc-badge", active && "active"]}>{text}</span>
	)
}
{variant === "dot" && <span class:list={["toc-dot", active && "active"]} />}
{
	variant === "dot-small" && (
		<span class:list={["toc-dot-small", active && "active"]} />
	)
}

<style>
	.toc-badge {
		display: inline-flex;
		align-items: center;
		justify-content: center;
		min-width: 1.25rem;
		height: 1.25rem;
		padding: 0 0.25rem;
		border-radius: 0.25rem;
		background: var(--toc-badge-bg);
		color: var(--btn-content);
		font-size: 0.75rem;
		font-weight: bold;
		flex-shrink: 0;
		transition: all 0.2s ease;
	}

	.toc-badge.active {
		background: var(--primary);
		color: white;
	}

	.toc-dot {
		display: inline-block;
		width: 0.5rem;
		height: 0.5rem;
		border-radius: 50%;
		background: var(--line-divider);
		flex-shrink: 0;
		transition: all 0.2s ease;
	}

	.toc-dot.active {
		background: var(--primary);
	}

	.toc-dot-small {
		display: inline-block;
		width: 0.35rem;
		height: 0.35rem;
		border-radius: 50%;
		background: var(--line-divider);
		flex-shrink: 0;
		transition: all 0.2s ease;
	}

	:global(.dark) .toc-dot-small {
		background: rgba(255, 255, 255, 0.1);
	}

	.toc-dot-small.active {
		background: var(--primary);
	}
</style>
